import { Link } from "react-router-dom";

const Public = () => {
    const content = (
        <section>
            This is the welcome section
        </section>
    )

    return content
}

export default Public;